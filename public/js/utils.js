// Generated by CoffeeScript 1.12.7
(function() {
  define(['jquery', 'underscore'], function($, _) {
    var U;
    return U = {
      cutLoc: function(loc) {
        var i, j, len, ls, ref, rt;
        rt = [];
        ls = loc.split('-');
        len = ls.length;
        for (i = j = 1, ref = len; 1 <= ref ? j <= ref : j >= ref; i = 1 <= ref ? ++j : --j) {
          rt.push(ls.slice(0, i).join('-'));
        }
        return rt;
      },
      formData: function($e) {
        var data;
        data = {};
        $e.find('[name]').each(function() {
          var name, val;
          name = $(this).attr('name');
          val = $(this).val();
          return data[name] = val;
        });
        return data;
      },
      query2obj: function(query) {
        var arr, obj;
        query = query.replace(/^\?/, '');
        obj = {};
        arr = query.split('&');
        arr.forEach(function(item) {
          var pics;
          pics = item.split('=');
          return obj[pics[0]] = pics[1];
        });
        return obj;
      }
    };
  });

}).call(this);
